# Business Artifact Reference Documents

This directory contains the authoritative source of business artifact knowledge.
These files are authored manually (not generated).

They are referenced by `data/artifacts.json` using stable IDs (ART-###).
