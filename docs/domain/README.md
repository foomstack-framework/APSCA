# Domain Reference Documents

This directory contains the authoritative source of domain knowledge.
These files are authored manually (not generated).

They are referenced by `data/domain.json` using stable IDs (DOM-###).
